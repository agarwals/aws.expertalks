# Setup the security group with the appropriate access for required inbound traffic

# Setup the specified number of instances for
	- AMI
	- the instance type
	- region
	- access credentials
	- security group
	
# Produce a manifest that lists the hostnames, IP addresses, and instance IDs as the output.

# Authorize the instances to the security group for access to the grinder console

# Run the grinder agent on the test instances

# Teardown the specified instances

END-OF-FILE
